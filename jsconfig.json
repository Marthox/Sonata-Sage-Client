{
  "compilerOptions": {
    "baseUrl": "src",
    "paths": {
      "$assets": ["assets"],
      "$components": ["components"],
      "$pages": ["pages"],
      "$router": ["router"],
      "$styles": ["styles"],
      "$layouts": ["layouts"]
    }
  }
}
